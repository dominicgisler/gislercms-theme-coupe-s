<!DOCTYPE html>
<html lang="{{ page.getLanguage().getLocale() }}">
    <head>
        {% include 'title.twig' %}
        {% include 'meta.twig' %}
        {% include 'style.twig' %}
    </head>
    <body>
        <div class="container my-4">
            <div class="rounded shadow">
                {% include 'header.twig' %}
                {% include 'main.twig' %}
                {% include 'footer.twig' %}
            </div>
        </div>
        {% include 'cookie-info.twig' %}
        {% include 'scripts.twig' %}
    </body>
</html>
